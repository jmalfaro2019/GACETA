generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Document {
  id            Int      @id @default(autoincrement())
  titre         String
  contenu_json  Json
  date_creation DateTime @default(now())

  @@map("documents")
}